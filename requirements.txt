### Core ML dependencies (install torch first)
numpy==1.26.3
torch # pinned transitively by vllm==0.11.0 -> torch==2.8.0 (was 2.4.0 under vllm 0.6.3)
torchvision # -> 0.23.0 (matches torch 2.8.0)
torchaudio # -> 2.8.0 (matches torch 2.8.0)

### vLLM and model dependencies
# vllm 0.11.0 adds Qwen3 (Qwen3ForCausalLM) support; 0.6.3 could not serve it.
# transformers is pinned to 4.57.0 (NOT 4.57.1+): a 4.57.x patch removed the
# `all_special_tokens_extended` tokenizer attribute (SpecialTokensMixin merge,
# transformers-v5 prep) that vllm 0.11.0's get_cached_tokenizer requires; 4.57.3
# crashes every server with "Qwen2Tokenizer has no attribute
# all_special_tokens_extended". 4.57.0 + vllm 0.11.0 is the tested-working combo
# and still has full Qwen3 support (>=4.51). Validate with: util/validate_vllm_qwen3.py
transformers[torch]==4.57.0
vllm==0.11.0
lm-format-enforcer
xformers==0.0.32.post1

### Database and server
psycopg2-binary==2.9.11
gpustat==1.1.1
mcp
starlette
uvicorn
asyncpg
pyairports
requests

### Requirements from MARFT (may need updating from time to time)
absl_py
cloudpickle
fairscale
json_repair
peft
pydantic
sentence_transformers
sentencepiece
setproctitle
tensorboard
tensorboardX
latex2sympy2
word2number

bert-score
matplotlib
seaborn
scikit-learn
